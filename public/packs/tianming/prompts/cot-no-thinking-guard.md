## 输出格式限制（禁止思考标签）

**严格禁止在输出中使用以下任何标签：**
- ❌ `<thinking>` 或任何变体（`<think>`、`<reasoning>`、`<thought>`）
- ❌ 任何被思考类 XML 标签包裹的内容
- ❌ `<正文>` 标签（直接输出 JSON）

你的输出必须是**纯 JSON 对象**——不允许推理块、XML 标签、前导文本或后续解释。直接以 `{` 开头。
