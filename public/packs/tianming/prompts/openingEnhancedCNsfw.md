   - **私密信息**（NSFW 模式已开启，**必填**）— 必须作为一个名为 `"私密信息"` 的**嵌套对象**放在每个 NPC 档案中，结构如下：
     ```json
     "私密信息": {
       "是否为处女/处男": true,
       "身体部位": [
         {"部位名称": "嘴", "敏感度": 40, "开发度": 10, "特征描述": "50-120字具体形态/触感", "特殊印记": ""},
         {"部位名称": "胸部", "敏感度": 60, "开发度": 20, "特征描述": "...", "特殊印记": ""},
         {"部位名称": "小穴", "敏感度": 50, "开发度": 0, "特征描述": "...", "特殊印记": ""},
         {"部位名称": "屁穴", "敏感度": 30, "开发度": 0, "特征描述": "...", "特殊印记": ""}
       ],
       "性格倾向": "...", "性取向": "...", "性癖好": ["..."],
       "性渴望程度": 30, "性交总次数": 0, "性伴侣名单": [],
       "当前性状态": "...", "体液分泌状态": "...", "特殊体质": []
     }
     ```
     - ⚠ 私密信息字段**绝对不能**平铺在 NPC 根级！必须嵌套在 `"私密信息": {...}` 对象内。
     - ⚠ `身体部位` 数组**必须包含** `嘴`、`胸部`、`小穴`、`屁穴` 这 4 个固定部位（可追加其他部位），每个部位的 `特征描述` 必须非空、禁止用"待生成/无"等占位词。
     - ⚠ 非处女/处男（`是否为处女/处男: false`）时**追加必填** `初夜夺取者`、`初夜时间`、`初夜描述`(50-200字) 三个字段。
     - ⚠ `是否为处女/处男: true` 时 → `性交总次数` 必须为 0 且 `性伴侣名单` 必须为 `[]`。
     - ⚠ 所有数值字段（敏感度/开发度/性渴望程度/性交总次数）必须是 number 类型，禁止用字符串如"无法计算"。
